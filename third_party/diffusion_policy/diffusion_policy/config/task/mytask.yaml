name: mytask

image_shape: [3, 240, 320]
dataset_path: /home/jinyang/yuwenye/human-in-the-loop/data/0220_PnP_new_gripper

shape_meta: &shape_meta
  # acceptable types: rgb, low_dim
  obs:
    wrist_img:
      shape: ${task.image_shape}
      type: rgb
    side_img:
      shape: ${task.image_shape}
      type: rgb
    qpos:
      shape: [7]
      type: low_dim
  action: 
    shape: [8]

env_runner:
  _target_: diffusion_policy.env_runner.myrunner.MyRunner

dataset:
  _target_: diffusion_policy.dataset.mydataset.MyDataset
  zarr_path: /home/jinyang/yuwenye/human-in-the-loop/data/0220_PnP_new_gripper/replay_buffer.zarr
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1'}
  pad_after: ${eval:'${n_action_steps}-1'}
  seed: 42
  val_ratio: 0.00
  max_train_episodes: 100000
  rel_ee_pose: True
  n_obs_steps: ${n_obs_steps}
